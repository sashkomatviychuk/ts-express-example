import * as path from 'path'

// public path
export const publicPath: string = path.join(__dirname, '..', '..', 'public');

// views path
export const viewsPath: string = path.join(__dirname, '..', 'views');
